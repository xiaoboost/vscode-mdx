/** 错误文本 */
export const errorName = {
  // ..
};

export const StringChars = {
  import: Array.from('import').map((char) => char.charCodeAt(0)),
  export: Array.from('export').map((char) => char.charCodeAt(0)),
  from: Array.from('from').map((char) => char.charCodeAt(0)),
};
